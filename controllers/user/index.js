// const { Router } = require("express");
// const router = Router();
// const ctrl = require("../user/user.ctrl");

// // 회원가입(POST)
// router.post("/register", ctrl.post_register);

// // 로그인(POST)
// router.post("/login", ctrl.post_login);

// module.exports = router;
